// For TypeScript, we can define what a Tweet object looks like
export interface Tweet {
  _id: string;
  likes: string;
  retweets: string;
  replies: string;
  created_at: string;
  link: string;
  text: string;
}
